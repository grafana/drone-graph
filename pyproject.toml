[project]
name = "drone-graph"
version = "0.1.0"
description = "Graph your Drone pipelines"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "graphviz>=0.20.3",
    "pyyaml>=6.0.2",
]
